#include "main.h"

/**
 * _abs - display absolute value of int
 * @r: integer to check
 * Return: 0 Success
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}

	return (0);
}
